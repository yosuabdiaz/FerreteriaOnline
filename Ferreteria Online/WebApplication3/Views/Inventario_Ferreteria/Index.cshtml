@model IEnumerable<WebApplication3.Inventario_Ferreteria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.activo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ferreteria.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Producto.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.activo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ferreteria.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_inventario_ferreteria }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_inventario_ferreteria }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_inventario_ferreteria })
        </td>
    </tr>
}

</table>
