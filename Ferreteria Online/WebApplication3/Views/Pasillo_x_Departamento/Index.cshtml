@model IEnumerable<WebApplication3.Pasillo_x_Departamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.activo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Departamento_x_Ferreteria.id_departamento_x_ferreteria)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pasillo.id_pasillo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.activo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Departamento_x_Ferreteria.id_departamento_x_ferreteria)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pasillo.id_pasillo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_pasillo_x_departamento }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_pasillo_x_departamento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_pasillo_x_departamento })
        </td>
    </tr>
}

</table>
