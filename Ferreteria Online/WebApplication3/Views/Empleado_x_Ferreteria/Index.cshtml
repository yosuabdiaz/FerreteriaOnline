@model IEnumerable<WebApplication3.Empleado_x_Ferreteria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.activo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Empleado.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ferreteria.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.activo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Empleado.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ferreteria.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_empleado_x_ferreteria }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_empleado_x_ferreteria }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_empleado_x_ferreteria })
        </td>
    </tr>
}

</table>
